<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.mapper.FilmMapper">
	<select id="searchFilm" resultType="FilmDto">
		SELECT fcode
		FROM films
		WHERE fname=#{fname} AND frelease=#{frelease}
	</select>
	<insert id="insertFilm">
		INSERT INTO films
		(fname, frelease, fdirector, fcast)
		VALUES
		(#{fname}, #{frelease}, #{fdirector}, #{fcast})
	</insert>
</mapper>