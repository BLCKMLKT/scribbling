<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.mapper.TagMapper">
	<select id="findTag" parameterType="string" resultType="string">
	   SELECT tname
	   FROM tag_library
	   WHERE tname LIKE CONCAT(#{value}, '%') 
	</select>
	<select id="searchTaglib" parameterType="string" resultType="integer">
		SELECT tid
		FROM tag_library
		WHERE tname=#{value}
	</select>
	<insert id="insertTaglib">
		<selectKey keyProperty="tid" order="BEFORE" resultType="integer">
			SELECT LAST_INSERT_ID()
		</selectKey>
		INSERT INTO tag_library
		(tname)
		VALUES
		(#{tname})
	</insert>
	
	<insert id="insertTag">
		INSERT INTO tags
		(sno, fcode, tid)
		VALUES
		(#{sno}, #{fcode}, #{tid})
	</insert>
</mapper>