<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.mapper.TagMapper">
	<select id="findTag" parameterType="string" resultType="string">
	   SELECT tname
	   FROM tag_library
	   WHERE tname LIKE CONCAT(#{value}, '%') 
	</select>
	<select id="searchTaglib" parameterType="TagVO" resultType="integer">
		SELECT tid
		FROM tag_library
		WHERE tname=#{tname}
	</select>
	<insert id="insertTaglib" parameterType="TagVO">
		INSERT INTO tag_library
		(tname)
		VALUES
		(#{tname})
		<selectKey keyProperty="tid" resultType="integer">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
	
	<insert id="insertTag">
		INSERT INTO tags
		(sno, fcode, tid)
		VALUES
		(#{sno}, #{fcode}, #{tid})
	</insert>
	<select id="listTags" parameterType="integer" resultType="string">
		SELECT tl.tname
		FROM tag_library as tl
		JOIN tags as t
		ON t.tid=tl.tid
		WHERE sno=#{value}
	</select>
</mapper>